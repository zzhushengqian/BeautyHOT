name: BeautyHOT Daily

on:
  push:
    branches: [main]
  workflow_dispatch:
  schedule:
    # GitHub Actions schedules are best-effort and can be delayed at the top
    # of the hour. These UTC entries are 07:50, 08:20 and 08:45 Beijing time.
    # Any one successful run refreshes the same daily archive.
    - cron: "50 23 * * *"
    - cron: "20 0 * * *"
    - cron: "45 0 * * *"

permissions:
  contents: write
  models: read
  pages: write
  id-token: write

concurrency:
  group: beautyhot-pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
      - name: Refresh daily feed
        if: github.event_name == 'schedule' || github.event_name == 'workflow_dispatch' || (github.event_name == 'push' && github.actor != 'github-actions[bot]')
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: python scripts/refresh_daily.py
      - name: Archive generated feed
        if: github.event_name == 'schedule' || github.event_name == 'workflow_dispatch' || (github.event_name == 'push' && github.actor != 'github-actions[bot]')
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data
          git diff --cached --quiet || git commit -m "daily: refresh BeautyHOT feed"
          git push
      - uses: actions/configure-pages@v5
        with:
          enablement: true
      - uses: actions/upload-pages-artifact@v4
        with:
          path: .
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
